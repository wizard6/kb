# 🛡️ 知识库安全公约 — Guard Rules

> 本文件是知识库的**操作契约**，每次操作前必须读取并遵守。
> 违反规则的修改将被阻止。

---

## 🔑 权限控制级别

知识库操作受以下三级权限控制，**权限从低到高**，高权限包含低权限的能力：

| 级别 | 标识 | 能力 | 说明 |
|------|------|------|------|
| 🔍 **读** | `read` | 查询已有资料、回答问题 | 不可创建/修改/生成任何文件 |
| ✏️ **写** | `write` | 读 + 按用户原话更新文档 | **只能写用户明确说的内容**，禁止 AI 自行生成 |
| 🧠 **生成** | `generate` | 读 + 写 + AI 自主生成文档 | 可基于理解生成新文档、分析、总结 |

### 行为对照

| 行为 | 读模式 | 写模式 | 生成模式 |
|------|--------|--------|----------|
| 查询/回答问题 | ✅ | ✅ | ✅ |
| 按用户原话写入条目 | 🚫 | ✅ | ✅ |
| AI 分析/总结/扩写 | 🚫 | 🚫 | ✅ |
| 更新索引 | 🚫 | ✅ | ✅ |
| 新建文件 | 🚫 | ✅（仅用户原文） | ✅ |
| 修改已有条目 | 🚫 | ✅（仅用户原文） | ✅ |

### 当前模式

> **默认：生成模式（`generate`）**
> 
> 当用户说「读一下」「查一下」「问个问题」等仅需查询时，自动降为 **读模式（`read`）**。
> 当用户说「记下来」「写上」「存到 KB」等明确写入指令时，按 **写模式（`write`）** 执行（保持原文不动）。
> 当用户说「分析」「总结」「生成」「写一份文档」等需要 AI 产出时，使用 **生成模式（`generate`）**。

## 🤔 「为什么」处理规则

当用户问「为什么」时（包括「为什么不」「为什么没」「怎么会」等），按以下顺序处理：

| 步骤 | 做什么 | 不做 |
|------|--------|------|
| ① 停下来 | 先不急着解释或解决 | ❌ 不要立刻给出原因 |
| ② 反思 | 想一下这个问题的背后反映什么问题 | ❌ 不要跳过反思直接行动 |
| ③ 发现根因 | 找出是哪个环节导致的（沟通不清？设计遗漏？操作失误？） | ❌ 不要只回答表面问题 |
| ④ 预防 | 想一下怎么避免以后再出现类似问题 | ❌ 不要只解决这一次 |
| ⑤ 改规则 | 把预防措施写入对应的 KB 文档或 `_guard.md` | ❌ 不要只说说而已 |
| ⑥ 解决 | 最后才处理当前的问题 | |

## 🔴 红线规则（AI 禁止自动执行）

| 操作 | 约束 | 说明 |
|------|------|------|
| ❌ **删除文件** | 🚫 禁止 AI 自动执行 | 必须经过用户**明确确认** |
| ❌ **删除目录** | 🚫 禁止 AI 自动执行 | 必须经过用户**明确确认** |
| ❌ **移动/重命名** | 🚫 禁止 AI 自动执行 | 必须经过用户**明确确认** |
| ❌ **清空/覆盖内容** | ⚠️ 超过 50% 内容变更需用户确认 | 部分编辑不受限 |

---

## ✅ 允许自动执行的操作

| 操作 | 说明 |
|------|------|
| ✅ 新建文件 | 直接写入新的知识条目，**必须保持用户原文不动**，不得添加解释/阐述/注释 |
| ✅ 追加/修改内容 | 在已有条目中更新信息 |
| ✅ 更新索引 `_index.md` | 维护索引属于常规维护 |
| ✅ 归档（移到 Archives/） | 非删除，移入归档区 |

## 🎯 价值判断标准（写入前评估）

写入新条目前必须评估**是否有知识价值**，只有以下类型值得写入：

| 有价值 | 说明 |
|--------|------|
| 🟢 **独特的看法/见解/角度** | 用户本人提炼的观点、判断、方法论 |
| 🟢 **用户的个人数据/焦点** | 与用户自身相关的信息、收藏、关注方向 |
| 🟢 **经典名句/金句** | 值得聚焦记录的原句、引语 |
| 🟢 **需要聚焦的词条** | 用户特意指出的关键词、概念凝练 |

| 不值得写入 | 说明 |
|-----------|------|
| 🔴 **百科式词条解释** | 到处都能查到的定义、概念通用说明，无独特价值 |
| 🔴 **常见常识** | 公开的通用知识，非用户独有的视角 |

---

## 📋 评审债条约（Review Debt Rule）

> 控制知识库中「写了但没审过」的文件数量，防止积累过多未确认的内容。

### 约束

| 规则 | 说明 |
|------|------|
| 🚫 **禁止新增** | 当**当前项目**（例如 `Projects/NovelWizard/`）内存在 **超过 5 个未评审文件** 时，禁止在该项目中添加新文件 |
| 📌 **仅限当前项目** | 此约束仅作用于当前操作的 PARA 项目目录，不影响其他 Areas / Resources / Archives |

### 评审状态定义

| 状态 | 标记方式 | 含义 |
|------|----------|------|
| ✅ **已评审** | 文件 frontmatter 中包含 `reviewed: <日期>` 字段 | 用户已确认内容正确 |
| ❌ **未评审** | 文件 frontmatter 中**没有** `reviewed` 字段 | 待用户审阅 |

### 操作流程

```
新增文件前：
  1️⃣ → 统计当前项目目录下所有 `.md` 文件
  2️⃣ → 筛选出 frontmatter 中缺少 `reviewed` 字段的文件
  3️⃣ → 计算未评审数量
  4️⃣ → 若 > 5：拒绝新增，列出未评审文件清单，请求用户审阅
  5️⃣ → 若 ≤ 5：允许新增

用户审阅后：
  → 在文件 frontmatter 中添加 `reviewed: YYYY-MM-DD`
  → 重新计数
```

### 例外

- 更新索引 `_index.md` 不受此约束限制
- 更新已有条目（追加/修改内容）不受此约束限制
- 归档操作不受此约束限制

---

## 📁 文件拆分约定

| 规则 | 说明 |
|------|------|
| 🔪 **按目标和单元拆分** | 计划类文件按「目标→单元」组织，不按「天/月」拆分。用户自主决定每天完成几个单元 |
| 📂 **单文件专注一件事** | 一个文件只做一件事。学习框架和学习计划分两个文件 |
| 🔗 **索引文件只做导航** | `_index.md` 只维护条目和标签，不包含实际内容 |
| 🚫 **不堆叠** | 当文件内容超过200行时，考虑拆分子文件或子目录 |

---

## 📤 GitHub 推送规则

| 规则 | 说明 |
|------|------|
| ⏰ **切换话题时推送** | 当用户提出和当前无关的新内容时，先将本次周期的改动提交并推送到 GitHub |
| 📦 **打包提交** | 同一话题的多次改动合并为一次 commit，不做零碎推送 |
| 🚫 **不自动推送** | 不在每次写入后自动推送，除非用户明确要求或话题切换 |

---

## 🔐 删除审批流程

当用户提出删除要求时，AI 必须执行以下步骤：

```
1️⃣    → 读取本公约 _guard.md
2️⃣    → 明确告知用户即将执行删除操作
3️⃣    → 列出待删除的文件清单
4️⃣    → 使用 request_user_input 请求用户二次确认
5️⃣ 确认 → 执行删除 + 更新索引
6️⃣ 拒绝 → 放弃操作，原样保留
```

---

## 📋 生效范围

- 知识库根目录：`knowledge-base/` 及其全部子目录
- 不适用于：工作区根目录下的临时文件、构建产物
